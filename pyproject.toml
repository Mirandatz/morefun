[tool.black]
target-version = ['py39']

[tool.pytest.ini_options]
testpaths = ["gge/tests"]
addopts = "--pspec"
filterwarnings = [
    "ignore::DeprecationWarning:tensorflow.*:",
    "ignore::DeprecationWarning:flatbuffers.*:"
]
