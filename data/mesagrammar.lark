start: layer+

layer        : (conv_layer | dense_layer | dropout_layer)

conv_layer   : "conv2d" filter_count kernel_size stride
filter_count : "filter_count" INT
kernel_size  : "kernel_size" INT
stride       : "stride" INT

dense_layer   : "dense" INT
dropout_layer : "dropout" FLOAT


%import common (INT, FLOAT)
%import common.WS

%ignore WS